import React, { memo } from 'react';
import Layout from '../Component/Layout'

const Home = memo(()=> {
    return (
        <Layout>
            <h1>Home</h1>
        </Layout>
    );
});

export default Home;
