import React, { memo } from 'react';
import Layout from '../../Component/Layout'

const Admin = memo(()=> {
    return (
        <Layout>
            <h1>Admin</h1>
        </Layout>
    );
});

export default Admin;
